import { ElementRef, Renderer2, OnDestroy, ErrorHandler } from '@angular/core';
import { ShowHideService } from './show-hide.service';
export interface ShowHideStatusConfig {
    id?: string;
    show?: string;
    hide?: string;
    materialIcon?: boolean;
}
export declare class ShowHideStatusDirective implements OnDestroy {
    private service;
    private el;
    private renderer;
    private errorHandler;
    private subscription;
    private config;
    showHideStatus: ShowHideStatusConfig;
    constructor(service: ShowHideService, el: ElementRef, renderer: Renderer2, errorHandler: ErrorHandler);
    private init;
    private updateStatus;
    ngOnDestroy(): void;
}
